const testVar = {};

function testFunc() {
  return "hi";
}

function superbowlWin(array) {
  let winRecord = array.find(record => record.result === "W");
  let result = win ? win.year : undefined;
  return result;
}
